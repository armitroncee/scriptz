<nav class="navbar navbar-expand-lg navbar-dark bg-dark">  
  <a class="navbar-brand" href="#"> My Pill Box</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01"
    aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
        <button type="button" class="btn btn-secondary">
      <li class="nav-item active">
        <a class="nav-link" href="/home">Home
          <span class="sr-only">(current)</span>
        </a>
      </li>
      </button>
      <button type="button" class="btn btn-secondary">
      <li class="nav-item">
        <a class="nav-link" id="pill-box" href="/pill-box">Pill Box</a>
      </li>
    </button>
    <button type="button" class="btn btn-secondary">
      <li class="nav-item">
        <a class="nav-link" href="/med-list">Medication List</a>
      </li>
    </button>
    <button type="button" class="btn btn-secondary">
      <li class="nav-item">
        <a class="nav-link" href="/sign-up">Sign Up </a>
      </li>
    </button> 
    </ul>
  </div>
</nav>

<div class="jumbotron">
  <h1 class="display-3">Medication Schedule</h1>
  <p class="lead"></p>
  <hr class="my-4">
</div>

<!-- Top Row -->
<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Morning</h4></div>
      <div class="card-body">
        <p class="card-text">Med: {{#each scriptz}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/each}} </p>
      </div>  
    </div>

    </div>
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Lunch Time</h4></div>
      <div class="card-body">
        <p class="card-text">Med: {{#each scriptzLunch}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/each}}</p>
      </div>  
    </div>      

    </div>
  </div>
</div>
<!-- Bottom Row -->
<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Dinner Time</h4></div>
      <div class="card-body">
        <p class="card-text">Med: {{#each scriptzDinner}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/each}}</p>
      </div>  
    </div>

    </div>
    <div class="col-md-6">
      <div class="card border-secondary mb-3">
      <div class="card-header"><h4>Before Bed</h4></div>
      <div class="card-body">
        <p class="card-text">Med: {{#each scriptzBed}}<button type="button" class="btn btn-primary" data-toggle="modal" data-target=".bd-example-modal-lg" value="{{this.prescription_name}}" id="medButton">{{this.prescription_name}}</button>{{/each}}</p>
      </div>  
    </div>      
    </div>
  </div>
</div>

<div class="container-fluid">
<div class="card border-secondary mb-3">
      <div class="card-header"><h4>Meds Taken</h4></div>
      <div class="card-body">
        <p class="card-text">Med: </p>
      </div>  
  </div>    
  </div> 
  <footer>
      <br><br><br>
    <div class="panel-footer" align="center"><a href="/about">About Scriptz</a></div>
  </footer>

<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      ...
    </div>
  </div>
</div>